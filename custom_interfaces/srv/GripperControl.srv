uint8 state # which sequence number to set (it can set upto 256 output)
uint8 data # data of the sequence number if needed
---
bool success   # indicate successful run of triggered service
string message # informational, e.g. for error messages